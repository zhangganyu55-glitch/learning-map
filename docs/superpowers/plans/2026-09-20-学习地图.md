# 学习地图 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 交付一个单文件 HTML 的学习进度看板，把用户 19 周的行动计划变成可勾选、可自定义、带可视化的地图，数据存在本机。

**Architecture:** 一个 `index.html`，内部用 `/* ===== CORE ===== */` 标记隔出一段不触碰 DOM 的纯函数区（数据与逻辑），其余是渲染层。测试用 Node 内置 `node:test` 从 HTML 里抽出 CORE 段执行，因此既保持"单文件双击可用"，又能对逻辑做真实回归。

**Tech Stack:** 原生 HTML/CSS/JS（ES2020）、Node 26 内置测试运行器。零第三方依赖、零 CDN。

**Spec:** `docs/superpowers/specs/2026-09-20-学习地图-design.md`

## Global Constraints

- 交付物必须是**单个** `index.html`，双击即开，离线可用，不引用任何外部 URL。
- 不引入任何 npm 依赖；测试只用 `node:test` 与 `node:assert`。
- CORE 区不得出现 `document`、`window`、`localStorage` 字样。
- 所有改状态的 CORE 函数必须是纯函数：返回新对象，不修改入参。
- 视觉令牌固定：底色 `#0b1020`→`#111834`，卡片 `rgba(255,255,255,.045)` + 描边 `rgba(255,255,255,.08)`，强调渐变 `#6366f1`→`#22d3ee`，主文字 `#e8ecf8`、次 `#98a2b8`、弱 `#5c6780`。
- 字体栈固定：`-apple-system, "PingFang SC", "Helvetica Neue", sans-serif`。
- 响应式断点 640px；`prefers-reduced-motion` 下关闭非必要动效。
- git 提交署名：`git -c user.name="张甘雨" -c user.email="2569419356@qq.com" commit`。
- localStorage 键名固定 `learning-map-state-v1`。

## File Structure

```
学习地图/
  index.html                        ← 唯一交付物（CORE + 样式 + 渲染 + 事件）
  tests/core.test.mjs               ← 抽 CORE 段执行的单元测试
  docs/superpowers/specs/...        ← 设计稿（已存在）
  docs/superpowers/plans/...        ← 本文件
  README.md                         ← 用法、备份、改数据的位置
```

---

### Task 1: 测试骨架与预期数据（RED）

**Files:**
- Create: `tests/core.test.mjs`

**Interfaces:**
- Produces: 对 CORE 区全部函数的签名与行为断言——后续任务都必须让这个文件变绿。

- [ ] **Step 1: 写测试文件**（内容见本任务末尾的代码块，完整照抄）
- [ ] **Step 2: 跑测试确认失败**

Run: `node --test tests/`
Expected: FAIL，报 `index.html` 不存在

- [ ] **Step 3: 放一个空壳 index.html，确认失败信息变成"找不到 CORE 标记"**

```bash
printf '<!doctype html><title>学习地图</title>\n' > index.html
node --test tests/
```
Expected: FAIL，`找不到 CORE 标记`

- [ ] **Step 4: 提交**

```bash
git add tests/core.test.mjs index.html
git -c user.name="张甘雨" -c user.email="2569419356@qq.com" commit -m "test: 学习地图核心逻辑测试骨架"
```

**测试文件全文：**

```js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { readFileSync } from 'node:fs';
import { fileURLToPath } from 'node:url';
import { dirname, join } from 'node:path';

const here = dirname(fileURLToPath(import.meta.url));
const html = readFileSync(join(here, '..', 'index.html'), 'utf8');
const m = html.match(/\/\* ===== CORE ===== \*\/([\s\S]*?)\/\* ===== CORE END ===== \*\//);
assert.ok(m, '找不到 CORE 标记');
const CORE = new Function(`${m[1]}
  return { createInitialState, findWeek, weekProgress, overallProgress, currentWeek,
    toggleTask, addTask, updateTaskText, deleteTask, moveTask, addWeek, removeWeek,
    setWeekNote, updateWeekMeta, toggleCheckin, streak, exportJSON, importJSON };`)();

const firstTask = (s) => s.weeks[0].tasks[0];

test('CORE 区的代码不含 DOM / 存储调用（预置数据文字除外）', () => {
  const code = m[1].replace(/var SEED_WEEKS = \[[\s\S]*?\n\];/, '');
  for (const k of ['document', 'window', 'localStorage']) {
    assert.ok(!code.includes(k), `CORE 代码里出现 ${k}`);
  }
});

test('预置数据：17 张卡覆盖 1..19 周且无缺口', () => {
  const s = CORE.createInitialState();
  assert.equal(s.weeks.length, 17);
  const nums = [];
  for (const w of s.weeks) for (let n = w.no; n <= w.noEnd; n++) nums.push(n);
  assert.deepEqual(nums, Array.from({ length: 19 }, (_, i) => i + 1));
});

test('预置数据：每周有任务、id 全局唯一', () => {
  const s = CORE.createInitialState();
  const ids = new Set();
  for (const w of s.weeks) {
    assert.ok(w.tasks.length >= 1, `${w.id} 没有任务`);
    for (const t of w.tasks) {
      assert.ok(t.text && t.text.length > 0);
      assert.ok(!ids.has(t.id), `重复 id: ${t.id}`);
      ids.add(t.id);
    }
  }
});

test('预置数据：第 1 周含"这周三件事"，第 6/8/10 周有检查点', () => {
  const s = CORE.createInitialState();
  assert.ok(s.weeks[0].tasks.some((t) => t.text.includes('20 行')));
  for (const no of [6, 8, 10]) {
    const w = s.weeks.find((x) => x.no === no);
    assert.ok(w.checkpoint && w.checkpoint.length > 10, `第 ${no} 周缺检查点`);
  }
});

test('createInitialState 每次返回独立副本', () => {
  const a = CORE.createInitialState();
  CORE.toggleTask(a, a.weeks[0].id, firstTask(a).id);
  const b = CORE.createInitialState();
  assert.equal(firstTask(b).done, false);
});

test('toggleTask 不可变且能来回切换', () => {
  const s = CORE.createInitialState();
  const w = s.weeks[0].id;
  const t = firstTask(s).id;
  const s2 = CORE.toggleTask(s, w, t);
  assert.equal(firstTask(s).done, false, '不得修改入参');
  assert.equal(firstTask(s2).done, true);
  assert.equal(firstTask(CORE.toggleTask(s2, w, t)).done, false);
});

test('weekProgress / overallProgress 与勾选同步', () => {
  let s = CORE.createInitialState();
  const w0 = s.weeks[0];
  assert.equal(CORE.weekProgress(w0).done, 0);
  assert.equal(CORE.overallProgress(s).weeksTotal, 19);
  assert.equal(CORE.overallProgress(s).weeksDone, 0);
  for (const t of w0.tasks) s = CORE.toggleTask(s, w0.id, t.id);
  assert.equal(CORE.weekProgress(CORE.findWeek(s, w0.id)).ratio, 1);
  assert.equal(CORE.overallProgress(s).weeksDone, 1);
});

test('两周共用的卡片完成后按 2 周计入', () => {
  let s = CORE.createInitialState();
  const multi = s.weeks.find((w) => w.noEnd - w.no === 1);
  assert.equal(multi.no, 15);
  for (const t of multi.tasks) s = CORE.toggleTask(s, multi.id, t.id);
  assert.equal(CORE.overallProgress(s).weeksDone, 2);
});

test('currentWeek 指向第一张未完成的卡，全完成后指向最后一张', () => {
  let s = CORE.createInitialState();
  assert.equal(CORE.currentWeek(s).no, 1);
  for (const t of s.weeks[0].tasks) s = CORE.toggleTask(s, s.weeks[0].id, t.id);
  assert.equal(CORE.currentWeek(s).no, 2);
  let all = CORE.createInitialState();
  for (const w of all.weeks) for (const t of w.tasks) if (!t.done) all = CORE.toggleTask(all, w.id, t.id);
  assert.equal(CORE.currentWeek(all).no, 19);
});

test('addTask：追加自定义任务，空文字忽略', () => {
  const s = CORE.createInitialState();
  const wid = s.weeks[2].id;
  const before = s.weeks[2].tasks.length;
  const s2 = CORE.addTask(s, wid, '自己加一条');
  assert.equal(s.weeks[2].tasks.length, before, '不得修改入参');
  assert.equal(s2.weeks[2].tasks.length, before + 1);
  assert.equal(s2.weeks[2].tasks.at(-1).kind, 'custom');
  assert.equal(s2.weeks[2].tasks.at(-1).done, false);
  assert.equal(CORE.addTask(s, wid, '   ').weeks[2].tasks.length, before);
});

test('updateTaskText / deleteTask', () => {
  const s = CORE.createInitialState();
  const wid = s.weeks[1].id;
  const tid = s.weeks[1].tasks[0].id;
  assert.equal(CORE.findWeek(CORE.updateTaskText(s, wid, tid, '改好了'), wid).tasks[0].text, '改好了');
  assert.equal(CORE.findWeek(CORE.updateTaskText(s, wid, tid, '  '), wid).tasks[0].text, s.weeks[1].tasks[0].text);
  const n = s.weeks[1].tasks.length;
  assert.equal(CORE.findWeek(CORE.deleteTask(s, wid, tid), wid).tasks.length, n - 1);
});

test('moveTask 上下移动且不越界', () => {
  const s = CORE.createInitialState();
  const wid = s.weeks[2].id;
  const [a, b] = s.weeks[2].tasks.map((t) => t.id);
  const up = CORE.findWeek(CORE.moveTask(s, wid, b, -1), wid).tasks;
  assert.deepEqual(up.slice(0, 2).map((t) => t.id), [b, a]);
  assert.deepEqual(CORE.findWeek(CORE.moveTask(s, wid, a, -1), wid).tasks.map((t) => t.id), s.weeks[2].tasks.map((t) => t.id));
  const last = s.weeks[2].tasks.at(-1).id;
  assert.deepEqual(CORE.findWeek(CORE.moveTask(s, wid, last, 1), wid).tasks.map((t) => t.id), s.weeks[2].tasks.map((t) => t.id));
});

test('addWeek / removeWeek（至少留一张卡）', () => {
  const s = CORE.createInitialState();
  const s2 = CORE.addWeek(s);
  assert.equal(s2.weeks.length, 18);
  assert.equal(s2.weeks.at(-1).no, 20);
  assert.equal(CORE.removeWeek(s, s.weeks[0].id).weeks.length, 16);
  assert.equal(CORE.removeWeek({ ...s, weeks: [s.weeks[0]] }, s.weeks[0].id).weeks.length, 1);
});

test('setWeekNote / updateWeekMeta', () => {
  const s = CORE.createInitialState();
  const wid = s.weeks[3].id;
  assert.equal(CORE.findWeek(CORE.setWeekNote(s, wid, '卡在闭包'), wid).note, '卡在闭包');
  const s2 = CORE.updateWeekMeta(s, wid, { title: '新标题', label: '10 月' });
  assert.equal(CORE.findWeek(s2, wid).title, '新标题');
  assert.equal(CORE.findWeek(s2, wid).label, '10 月');
  assert.equal(CORE.findWeek(CORE.updateWeekMeta(s, wid, {}), wid).title, s.weeks[3].title);
});

test('exportJSON → importJSON 往返等价，损坏数据抛错', () => {
  const s = CORE.createInitialState();
  const s2 = CORE.toggleTask(s, s.weeks[0].id, firstTask(s).id);
  assert.deepEqual(CORE.importJSON(CORE.exportJSON(s2)), s2);
  assert.throws(() => CORE.importJSON('{"weeks": "nope"}'));
  assert.throws(() => CORE.importJSON('不是 JSON'));
  assert.throws(() => CORE.importJSON('{"weeks": [{"id":"x"}]}'));
});

test('打卡：toggleCheckin 去重，streak 支持连续与断档', () => {
  let s = CORE.createInitialState();
  s = CORE.toggleCheckin(s, '2026-09-18', true);
  s = CORE.toggleCheckin(s, '2026-09-18', true);
  s = CORE.toggleCheckin(s, '2026-09-19', true);
  s = CORE.toggleCheckin(s, '2026-09-20', true);
  assert.equal(s.checkins.length, 3);
  assert.equal(CORE.streak(s, '2026-09-20'), 3);
  assert.equal(CORE.streak(s, '2026-09-21'), 3, '今天还没打卡时，昨天的连续仍然成立');
  assert.equal(CORE.streak(s, '2026-09-23'), 0);
  assert.equal(CORE.streak(CORE.toggleCheckin(s, '2026-09-19', false), '2026-09-20'), 1);
});
```

---

### Task 2: CORE 纯函数区（GREEN）

**Files:**
- Modify: `index.html`（在 `<script>` 内插入 CORE 段）

**Interfaces:**
- Produces: 上述测试需要的全部函数；界面层只通过它们改状态。

- [ ] **Step 1: 写预置数据**——17 张卡，逐字来自《五个月行动计划.md》：

```text
w1  no1           9 月下旬   第一批代码                 阶段 1 · JS 基础
    learn: 变量、数据类型、console.log、if、for
    learn: 顺手确认终端操作：cd、ls、mkdir，用 node xxx.js 跑一个文件
    learn: 打开 javascript.info 第一章，写满 20 行代码
    learn: 建一个 learning-log 仓库，把这 20 行提交上去
    learn: 把每天 1.5 小时的时段排进日历，设为周一至周五重复
    learn: 顺手修补：把 code 命令装进 PATH（Cmd+Shift+P → Shell Command: Install 'code' command in PATH）
    deliverable: 一个能在终端里玩的猜数字游戏，必须是你自己敲的

w2  no2   10 月上旬   函数
    learn: function、参数、return、作用域
    learn: 循环嵌套、字符串处理
    deliverable: 九九乘法表 + 一个成绩判定函数

w3  no3   10 月上旬   数组
    learn: 数组基础、遍历、push / pop
    learn: map / filter / reduce（这三个是重点，后面天天用）
    deliverable: 给一组成绩做处理——求平均、筛出及格、按分数排序

w4  no4   10 月中旬   对象
    learn: 对象、属性访问、嵌套对象
    learn: 解构、模板字符串
    deliverable: 用对象描述若干学生信息，写函数做统计

w5  no5   10 月中旬   函数进阶与模块
    learn: 箭头函数、回调、高阶函数（闭包理解即可，不要求精通）
    learn: import / export，把代码拆成多个文件
    deliverable: 把前几周的小练习拆成有结构的多个文件

w6  no6   10 月下旬   异步
    learn: Promise、async/await、fetch
    deliverable: 调用一个公开 API 并展示数据（天气、汇率、GitHub 用户信息皆可）
    checkpoint: 阶段 1 检查点：你现在应该能不看教程写出一个几十行的小工具。做不到就重做第 3、4 周，不要往下走。

w7  no7   11 月上旬   HTML 与 CSS              阶段 2 · 页面与上线
    learn: 语义化标签、盒模型、Flex 布局
    deliverable: 一个静态个人主页的页面

w8  no8   11 月中旬   部署上线 ★
    learn: Git 提交、推送到 GitHub
    learn: 开启 GitHub Pages，把你的页面部署上去
    deliverable: 一个能发给任何人的链接（手机也能打开）
    checkpoint: 这是整个五个月里最重要的一个里程碑。它意味着你第一次拥有了别人可以验证的成果。从这周起，你不再只是"在学"，而是"有作品"。

w9  no9   11 月中旬   DOM 与事件
    learn: querySelector、addEventListener、表单处理
    deliverable: 一个交互式小工具（番茄钟、待办清单、单位换算任选）

w10 no10  11 月下旬   让数据留下来
    learn: localStorage、数据与界面同步
    learn: 简单的错误处理
    deliverable: 上一个小工具加上数据保存，刷新不丢
    checkpoint: 阶段 2 检查点：你有一个自己写的、在线的、能用的工具。

w11 no11  12 月上旬   TypeScript 基础           阶段 3 · TS 与 React
    learn: 类型标注、接口、联合类型
    learn: 把第 10 周的工具改成 TS
    deliverable: 一个 TS 版本的交互工具

w12 no12  12 月中旬   React 入门
    learn: 组件、props、state、列表渲染
    deliverable: 用 React 重写那个工具

w13 no13  12 月下旬   期末周（编程降为最轻）
    learn: 主要精力给期末考试：这一学期不要挂科
    learn: 编程只保留每天 20 分钟，读代码，不写新功能
    deliverable: 考试不挂科

w14 no14  1 月上旬    寒假前                    过渡 · 期末收尾
    learn: 收尾期末考试
    learn: 开始想寒假要做的东西（只做想法，不动手）
    deliverable: 一个项目想法 + 三行验收标准

w15 no15  noEnd16  1 月下旬  寒假前段 · 搭骨架   阶段 4 · 完整项目
    learn: 选一个你自己真的会每天打开的东西（不要做待办清单和博客）
    learn: 先写"什么算做完"，再动手
    learn: 搭好项目结构，跑通最小可用版本
    deliverable: 能跑起来、但功能还很粗糙的雏形

w16 no17  noEnd18  2 月上旬  寒假后段 · 完成并上线
    learn: 补齐核心功能
    learn: 部署上线
    learn: 写 README，说明它做什么、怎么用
    learn: 注意：2027 年春节在 2 月 6 日，前后约一周效率会很低，提前预留缓冲
    deliverable: 一个上线的、你自己写的完整应用

w17 no19          2 月下旬   复盘与决定                 阶段 5 · 复盘与决定
    learn: 用一个周末认真回答那两个问题
    learn: 对着判断表做决定
```

- [ ] **Step 2: 写纯函数实现**（签名严格按 Task 1 的测试）
- [ ] **Step 3: 跑测试**

Run: `node --test tests/`
Expected: 全部 PASS，0 fail

- [ ] **Step 4: 确认 CORE 区没有 DOM 字样**（由 Task 1 里那条"CORE 区的代码不含 DOM / 存储调用"测试自动守住，跑 `node --test tests/` 即可）

- [ ] **Step 5: 提交**

```bash
git add index.html
git -c user.name="张甘雨" -c user.email="2569419356@qq.com" commit -m "feat: 学习地图核心逻辑与 19 周预置数据"
```

---

### Task 3: 界面骨架与视觉

**Files:**
- Modify: `index.html`（`<style>` 与 `<body>` 结构）

**Interfaces:**
- Consumes: `createInitialState / overallProgress / weekProgress / streak`
- Produces: 渲染层依赖的 DOM 容器：`#app`、`#ring`、`#ringLabel`、`#heat`、`#stageLabel`、`#streak`、`#weeks`、`#toast`、`#toolbar`

- [ ] **Step 1: 写样式令牌与布局**（顶栏 sticky / 工具条 / 周卡片 / 任务行 / 自绘复选框 / 640px 断点 / reduced-motion）
- [ ] **Step 2: 写 `<body>` 骨架**：顶栏（标题 + 当前阶段 + 19 格热力条 + 进度环 + 打卡天数）、工具条（导出 / 导入 / 全部展开 / 全部折叠 / 重置）、`<main id="weeks">`、列表下方"每周节奏"常驻提示块、`#toast`
- [ ] **Step 3: 画进度环**：SVG 圆环，`stroke-dasharray` 由 `overallProgress().ratio` 驱动
- [ ] **Step 4: 用预置数据渲染出整屏，肉眼确认视觉**（此时还没接勾选）
- [ ] **Step 5: 提交**

```bash
git add index.html
git -c user.name="张甘雨" -c user.email="2569419356@qq.com" commit -m "feat: 学习地图界面骨架与深色仪表盘视觉"
```

---

### Task 4: 交互接线

**Files:**
- Modify: `index.html`（渲染与事件）

**Interfaces:**
- Consumes: CORE 的全部状态函数
- Produces: 全局 `state`、`save()`、`render()`

- [ ] **Step 1: 状态与事件委托**：在 `#weeks` 上挂一个 click/change/input 委托处理器，按 `data-*` 分派到 CORE 函数
- [ ] **Step 2: 勾选**：`toggleTask` → 当天记一次 `toggleCheckin` → 重绘
- [ ] **Step 3: 就地编辑**：双击任务文字换成输入框，回车提交、Esc 取消；周主题与时间提示同理
- [ ] **Step 4: 增删改移**：任务行右侧 `↑ ↓ ✕`；每周底部"加一条"输入框，回车提交
- [ ] **Step 5: 卡点框**：`textarea` → `setWeekNote`，输入防抖 300ms 保存
- [ ] **Step 6: 加周 / 删周**，删周需要二次确认
- [ ] **Step 7: 人工验证**：勾选、编辑、增删、移动各做一遍，刷新后状态仍在 → 提交

```bash
git add index.html
git -c user.name="张甘雨" -c user.email="2569419356@qq.com" commit -m "feat: 学习地图交互（勾选、就地编辑、增删移动、卡点记录）"
```

---

### Task 5: 持久化、导入导出、异常兜底

**Files:**
- Modify: `index.html`

**Interfaces:**
- Consumes: `exportJSON / importJSON`、localStorage 键 `learning-map-state-v1`

- [ ] **Step 1: `load()` / `save()`**：读不到或解析失败时回落 `createInitialState()`，并在 `#toast` 显示可关闭提示
- [ ] **Step 2: localStorage 不可用**：写入失败时退化为内存态，提示"本次修改不会被保存"
- [ ] **Step 3: 导出**：`Blob` + `URL.createObjectURL` 下载 `学习地图-备份-YYYY-MM-DD.json`
- [ ] **Step 4: 导入**：`<input type="file">` 读文本 → `importJSON` → 成功替换并提示；失败提示"文件不是本软件的备份，或已损坏"且状态不变
- [ ] **Step 5: 重置**：二次确认后回到预置计划
- [ ] **Step 6: 验证**：导出 → 重置 → 导入 → 勾选与卡点原样回来 → 提交

```bash
git add index.html
git -c user.name="张甘雨" -c user.email="2569419356@qq.com" commit -m "feat: 学习地图本地持久化与 JSON 备份"
```

---

### Task 6: 响应式、验收与文档

**Files:**
- Create: `README.md`
- Modify: `index.html`

- [ ] **Step 1: 375px 宽度检查**：顶栏转两行、热力条横向滚动、无横向滚动条
- [ ] **Step 2: 逐条对照设计稿"验收标准"8 条**
- [ ] **Step 3: 写 README**：怎么打开、数据存哪、怎么备份、怎么加任务、怎么改预置数据
- [ ] **Step 4: 最终提交**

```bash
git add -A
git -c user.name="张甘雨" -c user.email="2569419356@qq.com" commit -m "docs: 学习地图使用说明；完成响应式与验收"
```
